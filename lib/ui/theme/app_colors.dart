import 'package:flutter/material.dart';

/// Palette keyed to the skeuomorphic app icon: graphite metal, signal blue, success green.
abstract final class AppColors {
  static const background = Color(0xFF121417);
  static const surface = Color(0xFF1A1D22);
  static const surfaceRaised = Color(0xFF23272E);
  static const surfaceInset = Color(0xFF0E1013);

  static const border = Color(0xFF2E343D);
  static const borderStrong = Color(0xFF3D4550);

  static const textPrimary = Color(0xFFF2F4F7);
  static const textSecondary = Color(0xFFB8C0CC);
  static const textMuted = Color(0xFF7A8494);

  static const accent = Color(0xFF3B82F6);
  static const accentSoft = Color(0xFF1E3A5F);
  static const accentGlow = Color(0xFF60A5FA);

  static const success = Color(0xFF34D399);
  static const successSoft = Color(0xFF163528);
  static const warning = Color(0xFFFBBF24);
  static const warningSoft = Color(0xFF3A2E12);
  static const danger = Color(0xFFF87171);
  static const dangerSoft = Color(0xFF3A1A1A);

  static const mono = Color(0xFFD1D9E6);
}
