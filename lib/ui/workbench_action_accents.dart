import 'package:flutter/material.dart';

/// Workbench project-action accents — app-specific, not shared ethan_ui tokens.
abstract final class WorkbenchActionAccents() {
  /// Line-age / authorship plate (violet).
  static const lineAge = Color(0xFFA78BFA);

  /// Commit / working-tree plate (teal).
  static const commit = Color(0xFF2DD4BF);
}
